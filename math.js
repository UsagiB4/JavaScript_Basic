var numA = 14;
var numB = 23.33;
var numC = 4;

var add = numA + numC // just an addition
var sub = numB - numA // just subtracting
var mult = numA * numC // Multiply
var div = numB / numC // Division
var pow = numA ** 2 // power of 2
var remainder = numA % numC // Remainder

/*Simple as that. :D */